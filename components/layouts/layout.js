export default function layout({children}){
    return (
        <>
            <header>
                <nav>bonjour les enfants</nav>
            </header>
            <main>
                {children}
            </main>
        </>
    )
}